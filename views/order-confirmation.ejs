<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Food Ordering</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .confirmation-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 60vh; /* Adjust as needed */
            text-align: center;
        }
        .confirmation-icon {
            font-size: 4rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container mt-5">
        <div class="confirmation-container">
            <div class="confirmation-icon">🎉</div> <!-- Or use an actual icon library -->
            <h1 class="mb-3"><%= title %>!</h1>
            
            <% if (locals.message && message.text) { %>
                <div class="alert alert-<%= message.type === 'error' ? 'danger' : 'success' %>" role="alert">
                    <%= message.text %>
                </div>
            <% delete locals.message; %> 
            <% } else { %>
                 <p class="lead">Thank you for your order! Your order #<strong><%= orderId %></strong> has been placed successfully.</p>
            <% } %>
            
            <p>We will contact you shortly to confirm the details and estimated delivery time.</p>
            <p class="mt-4">
                <a href="/" class="btn btn-primary">Continue Shopping</a>
                <!-- <a href="/my-orders" class="btn btn-outline-secondary">View My Orders</a> --> <!-- Future enhancement -->
            </p>
        </div>
    </div>

    <%- include('partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 