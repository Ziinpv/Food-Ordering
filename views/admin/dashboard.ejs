<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Food Ordering</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .stat-card {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: .25rem;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            text-align: center;
        }
        .stat-card h3 {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--primary-color);
        }
        .stat-card p {
            font-size: 1.1rem;
            color: var(--dark-color);
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-5">
        <h1 class="mb-4"><%= title %></h1>
        
        <div class="row">
            <div class="col-md-6">
                <div class="stat-card">
                    <h3><%= totalFoodItems %></h3>
                    <p>Total Food Items</p>
                </div>
            </div>
            <div class="col-md-6">
                <div class="stat-card">
                    <h3><%= totalOrders %></h3>
                    <p>Total Orders</p>
                </div>
            </div>
            <!-- Add more stat cards here as needed -->
        </div>

        <div class="mt-4">
            <h2>Quick Actions</h2>
            <a href="/add-food" class="btn btn-primary me-2">Add New Food Item</a>
            <a href="/" class="btn btn-secondary">View Menu (Manage Items)</a>
            <!-- Add more admin actions -->
        </div>

    </div>

    <%- include('../partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 